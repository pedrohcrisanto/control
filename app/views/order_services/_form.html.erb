<%= form_with(model: order_service, local: true) do |form| %>
    <% if order_service.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(order_service.errors.count, "error") %> prohibited this order_service from being saved:</h2>

            <ul>
                <% order_service.errors.full_messages.each do |message| %>
                    <li>
                        <%= message %>
                    </li>
                    <% end %>
            </ul>
        </div>
        <% end %>

            <div class="form-group">
                <label>Serviço</label>
                <%= form.collection_select :service_id , Service.where(user_id: current_user.id), :id, :name, {} , class:'form-control'%>
            </div>

            <div class="form-group">
                <label>Quantidade*</label>
                <%= form.number_field :amount , class:'form-control'%>
            </div>

            <div class="form-group">
                <label>Funcionario*</label>
                <%= form.text_field :employee , class:'form-control'%>
            </div>

            <div class="form-group">
                <label>Data*</label>
                <%= form.date_field :date , class:'form-control datepicker'%>
            </div>

            <div class="form-group">
                <label>Hora Incio*</label>
                <br>
                <%= form.time_select :starttime , class:'form-control'%>
            </div>

            <div class="form-group">
                <label>Hora Final*</label>
                <br>
                <%= form.time_select :endtime , class:'form-control'%>
            </div>

            <div class="form-group">
                <label>Detalhes</label>
                <%= form.text_area :detail , class:'form-control'%>
            </div>

            <div class="actions">
                <%= form.submit 'Confirmar', class:'btn btn-success'%>
            </div>
            <% end %>
                <br>