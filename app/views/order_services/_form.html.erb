<%= form_with(model: order_service, local: true) do |form| %>
  <% if order_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_service.errors.count, "error") %> prohibited this order_service from being saved:</h2>

      <ul>
      <% order_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.collection_select :service_id , Service.where(user_id: current_user.id), :id, :name, {} %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
  </div>

  <div class="field">
    <%= form.label :employee %>
    <%= form.text_field :employee %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :starttime %>
    <%= form.time_select :starttime %>
  </div>

  <div class="field">
    <%= form.label :endtime %>
    <%= form.time_select :endtime %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_area :detail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
